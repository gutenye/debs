#!/usr/bin/env bash
#
# Summary: Build the source package
#
# Usage: gutdeb source
#
# Invoke debuild -S

set -e
[ -n "$APP_DEBUG" ] && set -x
source "$APP_ROOT/libexec/lib.sh"

main() {
  debuild -S
}

main "$@"
